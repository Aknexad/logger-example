export const PORT = process.env.PORT || 8080;
export const OPENSEARCH_HOSTS = process.env.OPENSEARCH_HOSTS || '127.0.0.1';
console.log('🚀 ~ PORT:', PORT);
console.log('🚀 ~ OPENSEARCH_HOSTS:', OPENSEARCH_HOSTS);
